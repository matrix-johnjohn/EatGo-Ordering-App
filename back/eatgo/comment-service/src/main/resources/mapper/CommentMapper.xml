<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.eatgo.comment.mapper.CommentMapper">

    <!--发布评论-->
    <insert id="uploadComment">
        insert into user_comment (comment,user_id,dish_id) values (#{comment},#{userId},#{dishId})
    </insert>
</mapper>